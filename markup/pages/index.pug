doctype html
html(class="no-js", lang="ru")

  include ../blocks/head/head.pug
  include ../blocks/header/header.pug
  include ../blocks/footer/footer.pug

  include ../blocks/first-screen/first-screen.pug
  include ../blocks/education/education.pug
  include ../blocks/packages/packages.pug
  include ../blocks/questions/questions.pug

  head
    +head(head.defaults)
  body.page
    |%=symbols=%
    main.page__inner
      //- +header()
      +first-screen()
      +education()
      +packages()
      +questions()

    .page__bottom
      +footer()


    script(src='%=static=%js/separate-js/lazyload.min.js' defer)
    script(src='%=static=%js/separate-js/jquery.min.js' defer)
    script(src='%=static=%js/separate-js/slick.min.js' defer)
    script(src='%=static=%js/separate-js/jquery.mask.min.js' defer)

    script(src='%=static=%js/main%=hash=%%=min=%.js' defer)

